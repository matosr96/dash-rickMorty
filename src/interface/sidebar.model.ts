export interface SidebarMenuItem{
    id: string;
    label: string;
    icon: any;
    url: string;
}

export interface SidebarMenuCard{
    id: string;
    displayName: string;
    photoUrl: string;
    title: string;
    url: string;
}